pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Building the server"
            }
        }

        stage('Test') {
            steps {
                echo "Testing the server"
            }
        }
    }
}
