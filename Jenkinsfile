pipeline {
    agent any

    stage {
       stage('build') {
          steps {
                echo "building"
           }
       }
    stage {
       stage('build') {
          steps {
                 echo "building"
           }
       }
    }
}
